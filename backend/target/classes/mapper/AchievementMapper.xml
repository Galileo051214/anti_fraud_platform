<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.anti.mapper.AchievementMapper">

    <select id="selectByCode" resultType="com.anti.entity.Achievement">
        SELECT id, code, name, description, icon, score_reward, condition_type, condition_value, create_time
        FROM achievement
        WHERE code = #{code}
    </select>

    <select id="selectAllEnabled" resultType="com.anti.entity.Achievement">
        SELECT id, code, name, description, icon, score_reward, condition_type, condition_value, create_time
        FROM achievement
        ORDER BY id ASC
    </select>

</mapper>
