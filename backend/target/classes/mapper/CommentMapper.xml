<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.anti.mapper.CommentMapper">

    <select id="selectByPostId" resultType="com.anti.entity.Comment">
        SELECT c.*, u.nickname as author_name, u.avatar as author_avatar
        FROM comment c
        LEFT JOIN sys_user u ON c.user_id = u.id
        WHERE c.post_id = #{postId} AND c.status = 1 AND c.parent_id = 0
        ORDER BY c.create_time DESC
    </select>

</mapper>
